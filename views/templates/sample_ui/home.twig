{% extends 'page.twig' %}

{% block title %}
   Home example
{% endblock %}

{% block content %}
    <ul class="nav nav-tabs">
        <li><a href="/sample_ui"><i class="icon-home"></i> Start</a></li>
        <li><a href="/sample_ui/basic_form"><i class="icon-list-alt"></i> Basic Form</a></li>
        <li class="active"><a href="/sample_ui/login_form"> Login Form Example</a></li>
    </ul>

    <div class="row">
      <div class="span12">
        <h5>Data Received:</h5>
        <pre>{{ params }}</pre>
        <p>some text.</p>
        <h5>Message form Controller Action:</h5>
        {% if login_success %}
        <div class="alert alert-success">{{ message }} <a href="login_form"> Back to Form</a></div>
        {% else %}
        <div class="alert alert-danger">{{ message }} <a href="login_form"> Try Again</a></div>
        {% endif %}
      </div>
    </div>
{% endblock %}
